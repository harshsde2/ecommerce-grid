run = "npm run dev"
entrypoint = "server/index.ts"

[env]
DATABASE_URL = "${DATABASE_URL}"
PGHOST = "${PGHOST}"
PGDATABASE = "${PGDATABASE}"
PGUSER = "${PGUSER}"
PGPASSWORD = "${PGPASSWORD}"
PGPORT = "${PGPORT}"

[http]
method = "GET"
path = "/"